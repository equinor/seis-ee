version: "3.4"
services:
  seis_ee:
    image: seis-ee
#    entrypoint: bash
    build: .
    environment:
      DECIMATED_FILES_DEST: "/data/decimated_files"
    volumes:
      - .:/data
#       Update this line with your raw data location
#      - ./test_data/:/raw
      - /project:/raw